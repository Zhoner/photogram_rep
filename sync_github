------------------------------------------------------------------
sync workspace, ogni volta che si sincronizza, a un nuovo progetto
------------------------------------------------------------------
git init
git add .
git remote add orgin https://github.com/Zhoner/photogram_rep.git
git remote -v
git pull --allow-unrelated-histories origin master
git push origin master
git commit -m "server"


------------------------------------------
sync with github, ogni modifica effettuata
------------------------------------------
git add .
git pull --allow-unrelated-histories orgin master
git push origin master
git commit -m "commnet"
------------
commands
------------
git ls-files -> view the files
